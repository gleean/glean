[package]
name = "glean-core"
version = "0.1.0"
edition = "2021"
description = "Glean core engine library (indexing pipeline, storage)."
license = "Apache-2.0"

[lib]
path = "src/lib.rs"

[features]
default = ["fastembed"]
fastembed = ["dep:fastembed"]

[dependencies]
arrow-array = "57"
arrow-schema = "57"
dirs = "6"
fastembed = { version = "5", optional = true }
futures = "0.3"
lance-index = "4.0"
lancedb = "0.27"
rusqlite = { version = "0.39", features = ["bundled"] }
sha2 = "0.10"
thiserror = "2"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "sync"] }
tracing = "0.1"
walkdir = "2"

[dev-dependencies]
tempfile = "3"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
