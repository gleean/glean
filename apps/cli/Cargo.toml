[package]
name = "pds"
version = "0.1.0"
edition = "2021"
description = "private-doc-stack CLI：pds"
default-run = "pds"

[[bin]]
name = "pds"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
parser-core = { path = "../../packages/parser-core" }
